import minimist from "minimist";
import { NetworkClient } from "@openzeppelin/defender-sdk-network-client";
export declare function getAndValidateString(parsedArgs: minimist.ParsedArgs, option: string, required?: boolean): string | undefined;
/**
 * Gets the network name for the given chainId.
 *
 * @param chainId Chain ID
 * @param networkClient Overrides the default network client. For testing only.
 * @param requireNetwork Overrides the DEFENDER_NETWORK environment variable. For testing only.
 * @returns Network name
 */
export declare function getNetwork(chainId: number, networkClient?: NetworkClient, requireNetwork?: string): Promise<string>;
export declare const USAGE_COMMAND_PREFIX = "Usage: npx @openzeppelin/defender-deploy-client-cli";
//# sourceMappingURL=utils.d.ts.map